const fs = require('fs');
const roomOptions = JSON.parse(fs.readFileSync('./data/rooms.json', 'utf8'));

const rooms = (req, res) => {
    pageTitle = "Travlr Getaways - Accomodations";
    res.render('rooms', { title: pageTitle, roomOptions });

}

module.exports = {
    rooms
}